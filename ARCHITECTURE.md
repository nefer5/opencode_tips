# opencode_tips 架构设计

> 本文档记录 opencode_tips 仓库的架构设计，确保长期维护不跑偏。
> 创建时间：2026-02-23

## 1. 设计目标

- **双层文档**：人类读友好版，Agent读结构化版
- **内容灵活**：不限于SKILL，可以是技巧、提示词、笔记
- **Token节约**：通过发布时间+缓存机制，避免Agent重复读取
- **极简读取**：使用 webfetch 读取 GitHub Raw URL，无需任何环境配置

## 2. 内容类型

| 类型 | 目录 | 说明 |
|------|------|------|
| `tip` | `tips/` | 短技巧、小贴士 |
| `prompt` | `prompts/` | 提示词模板 |
| `note` | `notes/` | 笔记、心得 |
| `skill` | `skills/` | 完整SKILL格式（可选） |

## 3. 目录结构

```
opencode_tips/
├── README.md                    # 👧 人类友好入口（你只看这个）
├── ARCHITECTURE.md             # 本文档
├── _index/
│   ├── index.md               # Agent主索引
│   └── _registry.yaml         # 内容注册表（所有内容的清单+发布时间）
├── tips/                      # 技巧类
│   └── 001-xxx.md
├── prompts/                   # 提示词类
│   └── 001-xxx.md
├── notes/                     # 笔记类
│   └── 001-xxx.md
├── skills/                   # SKILL类（可选）
│   └── 001-xxx.SKILL.md
├── _skill/
│   └── oc-tips-reader.SKILL.md  # 读取本仓库的规范SKILL
└── .gitignore
```

## 4. 核心文件规范

### 4.1 _registry.yaml - 内容注册表

```yaml
version: 1.0
last_published: "2026-02-23"
update_schedule: "周一、四"

contents:
  - id: tip-001
    type: tip
    title: "xxx技巧"
    file: tips/001-xxx.md
    published: "2026-02-20"
    status: published  # published / draft / deprecated
    tags: [tag1, tag2]
```

### 4.2 技巧/提示词文件格式

```yaml
---
title: 技巧标题
type: tip  # tip/prompt/note/skill
published: 2026-02-20
status: published
tags: [tag1, tag2]
---

# 技巧标题

## 内容...
```

## 5. Agent读取流程

1. **首次**：读取 README.md → 看到需要安装 oc-tips-reader.SKILL
2. **安装SKILL**：加载 `_skill/oc-tips-reader.SKILL.md`
3. **读取注册表**：用 webfetch 读取 `_index/_registry.yaml` 的 Raw URL
4. **对比时间**：对比本地缓存 `last_read` vs 远程 `last_published`
5. **读取内容**：有更新则用 webfetch 读取具体技巧文件
6. **按类型处理**：tip→技巧，prompt→模板，note→笔记

## 6. 缓存机制

Agent本地缓存（根据实际配置路径）：

```json
{
  "last_read": "2026-02-20",
  "last_published": "2026-02-20",
  "contents_read": ["tip-001", "tip-002"]
}
```

Raw URL 格式：
- `https://raw.githubusercontent.com/{用户}/{仓库}/main/{文件路径}`

## 7. 更新频率

- **检查频率**：由使用方 Agent 和用户讨论决定
- **读取方式**：webfetch 直接读取 GitHub Raw URL，无需任何环境配置

## 8. 维护原则

1. 所有内容必须有 `published` 和 `status` 字段
2. 新增内容必须在 `_registry.yaml` 中注册
3. 过时内容标记 `status: deprecated`
4. README 保持人类友好，不超过3层目录深度

---

*本架构由 YKD 设计，我审定。*
